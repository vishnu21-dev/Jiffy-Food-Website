server:
  port: 8089
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/AdminDb
  application:
    name: adminService
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true

